pipeline {
    agent {label 'Deploy'}

    stages {
        stage('Run Script') {
            steps {
                sh 'echo "successful"'
                sh 'echo $HELLO'
                sh 'echo $H'
                
            }
        }
    }
}
